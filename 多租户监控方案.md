# 竞品监控 · 多租户与权限方案（日后优化）

> 定位：本文属于**日后优化**范畴。当前 demo 主线是「单用户 + kids bicycle 样例（架构支持任意类目）」，先把单条链路跑顺、能现场演示即可。
> 但当监控要服务**多个运营、多类目**时，就需要多租户设计。本文给出两种模式（均已纳入），并给出推荐演进路径。
> 核心结论先放：**一个共享 Bot 服务所有运营，不要每人建一个 Bot**；类目与通知用「配置」表达，数据隔离用「行权限/视图」。

---

## 〇、角色澄清：Bot / Skill / 自动化 是什么关系

容易误以为"用 Skill + 自动化"就把 Bot 架空了。其实三者是**分层协作，不是互相替代**：

- **Bot（飞书应用 cli_aae6）= 接口层 + 身份层**：它是运营的统一入口（对话 / 指令），也是唯一持有「飞书写权限」的身份。没有 Bot，Skill 即便抓到数据也写不进 Base。
- **Skill（GitHub 工程）= 无状态引擎**：负责"给定类目 → 抓取 + 格式化"。它不绑定用户，可被 Bot 或定时任务反复调用。
- **自动化 = 触发器**：只是"定时让 Bot 跑一次 Skill"。它触发的是 Bot→Skill 这条链路，而不是绕过 Bot。

正确心智模型：**用户 / 定时 → 都打到 Bot → Bot 调 Skill → Skill 借 Bot 身份写回 Base**。Bot 是门面，Skill 是后厨，自动化是"到点开张"的闹钟；按需和定时走的是同一条链路。

> 这也回答"Skill 不能面向多用户"的担心：Skill 是无状态代码，多用户由 **Bot 识别 open_id + 数据打标** 实现，引擎完全可复用，没有"Skill 单用户"的硬限制。Bot 直接暴露指令接口（如"监控 自行车"）正是它作为产品前门的价值。
>
> 关于 Bot / Skill / 自动化三者的 API 身份关系，以及 WorkBuddy 飞书集成槽只能绑一个 Bot 带来的限制，详见 [`飞书API与Bot集成说明.md`](./飞书API与Bot集成说明.md)。

---

## 一、三条核心原则

1. **单实例共享 Bot**：全公司共用你这一个飞书应用（`App ID: cli_aae6d6fabe3adbe8`）作为服务身份。每人建一个 Bot = N 套 App 身份 + 凭证 + 权限校验 + Amazon 限流风险，纯反模式。
2. **权限管"看"，配置管"抓 + 通知"**：行权限/视图只能控制"谁能看见哪些行"，控制不了"抓哪些类目、降价了该推给谁"。通知路由必须显式记录"类目 → 负责人"。
3. **数据隔离用行权限，不是分库分 Bot**：运营 A 只看 owner=A 的行，而不是给他单独一个 Base/Bot。

---

## 二、模式 A：自助订阅（运营自服务）

运营通过 Base **表单视图**自己填一行订阅，单 Bot 每天扫配置表、对每一行跑抓取。

### 架构
```
运营(表单视图填订阅) → 订阅配置表(Base)
                         ↓ 单 Bot 定时读取
                  逐条抓取 Amazon → 写「竞品快照表」
                         ↓ 按配置里的「通知对象」
                  推送告警到对应运营 / 群
```

### 「订阅配置表」字段
| 字段 | 说明 |
|---|---|
| 订阅 ID | 自增/唯一 |
| 运营 | open_id + 姓名 |
| 类目 | node_id + 类目名称 |
| 频率 | 每日 / 每周 / 自定义 |
| 通知对象 | 群 chat_id 或 个人 open_id |
| 状态 | 启用 / 暂停 |
| 创建时间 | — |

### 适用 & 取舍
- ✅ 类目多、需自助扩展；运营主动性强，管理员不背类目清单。
- ⚠️ 运营有填写成本；配置质量依赖运营（填错 node_id 会抓偏）。

---

## 三、模式 B：管理员分配 + 行权限（运营零配置）← 推荐近期采用

你（管理员）统一维护「类目 × 负责人」表，单 Bot 抓全类目写一张快照表；行权限让每人只看自己的类目；「负责人」字段同时作为通知路由。

### 架构
```
管理员维护「类目×负责人」表 → 单 Bot 抓全类目 → 写「竞品快照表」
                                      ↓
                          行权限(负责人=当前用户) → 各运营只看自己的类目
                                      ↓
                          按「负责人/通知群」推送降价/异常
```

### 「类目 × 负责人」表字段
| 字段 | 说明 |
|---|---|
| 类目 | node_id + 类目名称 |
| 负责人 | open_id（同时用于行权限 + 通知路由） |
| 通知群 | chat_id（可选，异常汇总发群） |
| 状态 | 启用 / 暂停 |

> 一个字段（负责人）同时解决「可见隔离」与「通知路由」，不用搞两套配置。

### 行权限设置（飞书 Base）
- 方式一：**行权限** → 规则「负责人 包含 当前用户」，每人仅见自己的行。
- 方式二：**视图筛选** → 给每位运营建一个按负责人过滤的视图。
- 仪表盘引用同一张快照表，配合行权限即可实现"每人看自己的看板"。

### 适用 & 取舍
- ✅ 类目少而稳、你想控全局、运营非技术零配置；最贴合中小团队。
- ⚠️ 类目清单由你维护，类目暴涨时你会成瓶颈（此时再切模式 A）。

---

## 四、两种模式对比

| 维度 | 模式 A 自助订阅 | 模式 B 管理员分配 + 行权限 |
|---|---|---|
| 类目范围 | 运营自己加 | 管理员统一维护 |
| 运营操作 | 要填订阅表 | 零操作，直接看 |
| 可见隔离 | 行权限 / 视图 | 行权限 / 视图 |
| 通知路由 | 订阅表「通知对象」 | 「负责人」字段复用 |
| 配置质量风险 | 在运营（易填错） | 在管理员（可控） |
| 适合阶段 | 类目多、需自助扩展 | 类目少而稳、控全局 |

---

## 五、推荐演进路径

```
当前 demo（单用户 / kids bicycle 样例，架构支持任意类目）
   ↓ 先跑通多类目 + 权限隔离
模式 B（管理员分配 + 行权限）  ← 近期落地
   ↓ 类目暴涨 / 运营要自助
模式 A（开放自助订阅）
```

> 架构上两层解耦：无论 A 还是 B，**采集/存储/智能/展示四层不变**，只是"类目来源"从管理员表变成订阅表。未来要换 PostgreSQL + BI 大屏也不影响上层。

---

## 六、飞书落地清单（模式 B 为例）

1. 新建「类目 × 负责人」表，按 §三 字段建列。
2. 在 `run_pipeline.py` 增加"读配置 → 逐条抓取 → 写快照表"的循环（当前按 `category_directory.json` 目录爬，仅 `kids bicycle` 为 active 种子；任意类目经流 B 动态解析后写入目录复用）。
3. 快照表增加「负责人」冗余列（与类目表对齐），便于行权限过滤。
4. Base「行权限」设规则：负责人 包含 当前用户。
5. 异常告警按「负责人 / 通知群」推送（脚本读配置决定 chat_id）。
6. 仪表盘引用同一快照表，行权限自动让每人看自己的看板。

---

## 七、面试讲点（背这段）

> "我把它设计成**单实例多租户**：一个服务身份服务所有运营，租户差异用配置表表达，数据隔离靠行权限而非分实例——避免 Bot 泛滥，也方便统一限流和审计。近期用管理员分配模式让运营零配置上线，类目规模上来后再开放自助订阅，两层切换不影响采集/存储/智能/展示的解耦架构。"

这段同时展示了：**单实例多租户思维、配置驱动、行权限隔离、统一限流审计、渐进式演进**——B 端 AI PM 面试官看重的点。
