# Dify 竞品监控 Chatflow 搭建指南（备选方案）

> ⚠️ **最新决策（2026-08-14）**：竞品监控的**主交付改为飞书主动推送**（`feishu_report.py`：每天抓取完自动把日报/异常卡片推到飞书群给运营看），**不走 Dify 问答**；**Dify 保留纯做 listing**。
> 本文档是「运营在 Dify 里问答竞品」的**可选形态**——仅当未来想做"独立助手"时才启用，当前不作为主线。
> 数据后端仍是 `serve.py` 的 `GET /query`；推送版见 `feishu_report.py`。

---

## 〇、前置条件（已具备）
- 云端服务 `serve.py` 已部署到 Render，且新增了 `GET /query` 接口。
- 接口地址：`https://bsr-monitor.onrender.com/query?token=sooya1030`
- 该接口返回 JSON：`{stats, brands, anomalies, latest}`，数据全部来自飞书 Base。
- 你已有 Dify 使用经验（之前做过 listing Chatflow），本指南聚焦"监控"这个新应用。

---

## 一、整体架构（4 节点）

```
[开始] 用户输入(sys.query)
   │
   ▼
[工具] 竞品数据查询  →  GET https://bsr-monitor.onrender.com/query?token=sooya1030
   │   返回原始 JSON 存入变量 {{#tool.data#}}
   ▼
[LLM] 回答生成  →  结合 {{#sys.query#}} + {{#tool.data#}} 生成中文回答
   │
   ▼
[直接回复] 输出 LLM 结果
```

> 设计取舍：用"显式 工具节点 + LLM 汇总"而非 ReAct Agent，是为了**演示稳定、可控、好截图**。
> 两种都行，显式流更适合面试 demo（每一步可见，不会跑偏）。

---

## 二、在 Dify 里一步步建

### 步骤 1：新建 Chatflow
Dify 控制台 → 工作室 → **创建应用** → 选 **Chatflow**（对话型）→ 命名"亚马逊竞品监控助手"。

### 步骤 2：加 API 工具（关键）
1. 进入应用 → 编排页面 → 点 **"+ 工具"** 或左侧「工具」→ **创建自定义工具 / API 工具**。
2. 填写：
   - 工具名称：`竞品数据查询`
   - 描述：`查询飞书 Base 中的亚马逊 Best Sellers 竞品监控数据，返回最新快照、品牌分布、异常清单与统计。当用户问竞品排名/价格/异常/周报时调用。`
   - 方法：`GET`
   - URL：`https://bsr-monitor.onrender.com/query`
   - 参数：添加 `token`，类型 String，默认值 `sooya1030`，必填 ✔
3. 保存并在编排里把该工具作为 **Tool 节点** 拖入，连接 [开始] → [工具]。
4. 工具输出变量默认叫 `data`（完整 JSON 字符串）。

### 步骤 3：加 LLM 节点（回答生成）
1. 在工具节点后加一个 **LLM 节点**（模型选你可用的，如 gpt-4o-mini / 通义 / deepseek 均可）。
2. 上下文变量：
   - `{{#sys.query#}}`（用户原问题）
   - `{{#竞品数据查询.data#}}`（工具返回，变量名以你命名显示为准）
3. 把下方「系统提示词 / Prompt」整段粘贴进去。

### 步骤 4：直接回复
LLM 节点后接 **直接回复** 节点，内容填 `{{#llm.text#}}`（LLM 输出变量）。

### 步骤 5：发布
点右上角 **发布** → 即可在「预览 / Web App / API」里试用。

---

## 三、LLM 提示词（直接复制）

```
你是一个跨境电商竞品监控助手，基于下方【实时数据】用中文回答运营的问题。
你只做"数据翻译与解读"，不编造任何数据。

【数据字段说明】
- stats: 整体统计。latest_date=最新抓取日；days_covered=覆盖天数；brand_count=品牌数；
         price_median=价格中位数(USD)；avg_rating=平均评分；missing_flag_count=数据缺失标记数。
- brands: 各品牌席位与均价/均评，已按席位降序。
- anomalies: 异常清单，type 取值为 降价/涨价/排名上升/排名下降/新上榜/跌出榜单，detail 含变化细节。
- latest: 最新一日完整榜单（排名、ASIN、品牌、价格、评分、评论数、标题）。

【回答规则】
1. 严格基于提供的数据，绝不虚构数字或 ASIN。
2. 用口语化中文，站在"帮运营做决策"的角度。
3. 问"异常/变化/谁动了"：优先列举 anomalies，并给一句可执行建议
   （如"建议盯一下 WEIZE 的降价，可能抢份额"）。
4. 问"周报/总结/本周概况"：把 stats + brands + anomalies 汇总成一段≤150字业务周报，
   格式：①整体 ②品牌格局 ③重点异常 ④建议。
5. 金额一律带 USD（$），排名说"第 X 名"。
6. 数据不足或字段为空时，如实说明，不要硬编。
7. 如果用户问题模糊，先给最相关的核心结论，再补充细节。

用户问题：{{#sys.query#}}

实时数据（JSON）：
{{#竞品数据查询.data#}}
```

> 提示词里 `{{#竞品数据查询.data#}}` 的节点名要和你实际拖入的工具名一致，Dify 里点变量会自动带出正确引用。

---

## 四、面试怎么讲（背这段）

> "采集和存储跑通后，我把价值重心放在**智能层**。我让 Dify 作为一个自然语言入口：
> 运营不用写筛选、不用看表，直接问'本周谁降价了'，Dify 调我云端暴露的数据接口拿到实时数据，
> 由 LLM 翻译成人话并给可执行建议。异常检测（降价/排名波动/新上榜）是规则层先算好的，
> LLM 负责'解读+周报'，两者分工——**规则保证准确，LLM 保证好懂**。
> 这比纯表格多了'决策辅助'这层，也是这个产品区别于普通爬虫的核心。"

这段话同时展示了：用户导向、LLM 应用设计、规则+模型分工、知道什么该用规则什么该用模型。

---

## 五、能力边界与后续增强（日后优化）
- **当前**：查询型（运营问才答）。数据来自 `/query`，实时性 = 每天 18:00 抓取后。
- **增强 1 主动告警**：飞书 Base 自动化在抓取完成后，再 POST 一个 `/report` 接口 → 云端算 anomalies → 推送到运营群。无需人工问。
- **增强 2 多类目**：`/query` 支持 `?category=xxx` 参数，按类目过滤（需先扩展 `run_pipeline` 多类目）。
- **增强 3 语音/群聊入口**：把 Dify 的 Web App 嵌入飞书群机器人，运营在群里 @ 就问。

---

## 六、部署提醒（更新 serve.py 后必做）
你修改了 `serve.py`（新增 `/query`），必须让 Render 用上新代码：
1. 打开 GitHub 仓库 `bsr-monitor` → 找到 `serve.py` → **Edit / 上传替换**为新版本（因你最初是网页拖拽上传，重新传一次即可）。
2. 提交后 Render 会自动重新部署（仓库连着 Render）。
3. 部署完成后，浏览器开 `https://bsr-monitor.onrender.com/query?token=sooya1030` 应看到 JSON。
4. 若 Render 没自动部署，去 Render 控制台该服务点 **Manual Deploy → Deploy latest commit**。
