# PRD：亚马逊多类目 Listing 智能生成与质检系统

> 文档类型：产品需求文档（PRD）｜版本：v1.0（作品集版）｜状态：MVP 已跑通验证
> 作者角色：AI 产品经理（3 年跨境电商运营背景）

---

## 1. 背景与问题

基于 3 年亚马逊运营经验，中小卖家在 Listing 环节存在以下核心痛点：

- **质量决定生死**：Listing 标题/五点/搜索词直接影响 A9 搜索排名与自然转化率，但多数卖家缺乏专业文案与 SEO 能力。
- **合规红线易踩**：标题字符限制、类目错放、禁词、参数虚标都可能导致降权或下架。
- **竞品分析低效**：手工爬 BSR 榜单耗时、难持续，且易被反爬阻断。
- **现有 AI 工具"幻觉严重"**：通用大模型写文案常编造品牌、材质、电池容量等硬事实，卖家直接上架有违规风险。

## 2. 产品目标

### 2.1 目标
让卖家输入**一句自然语言类目**（如"游戏手柄""不锈钢保温水杯"），即可生成**参考真实竞品、合规、可达上架标准**的亚马逊 Listing，且**绝不编造硬事实**。

### 2.2 范围
- **In Scope（MVP）**：自然语言类目解析、实时竞品抓取、Listing 生成（标题/五点/描述/后台搜索词）、四维质检、多轮回炉闭环、诚实回问。
- **Out of Scope**：多语言翻译、广告/A-B 投放联动、订单数据接入、**真 Agent 自主规划**（列入路线图）。

## 3. 用户与场景

**用户画像**
- 主用户：亚马逊中小卖家 / 运营（缺乏专业文案能力）
- 次用户：品牌出海团队（需要批量生成与优化）

**典型场景**
1. 选品后快速产出首版 Listing
2. 现有 Listing 优化：输入类目 + 现有文案 → 输出质检报告 + 改稿建议
3. 竞品监控：定期抓取类目 BSR 头部，辅助定价与选品决策

## 4. 核心功能需求（FR）

**FR-1 自然语言类目解析**
- 输入任意中文/英文类目短语，解析为亚马逊真实类目 `node_id`
- 三源交叉验证（面包屑 / zgbs / node 链接），**绝不编造 node_id**
- 低置信结果 → 走人工确认（HITL）

**FR-2 实时竞品抓取**
- 同步抓取该类目 BSR 头部 `topk`（默认 8）商品：品牌、价格、标题、评分、评论数、五点
- 反爬兜底（完整浏览器头）+ 失败重试 + 本地缓存

**FR-3 Listing 生成**
- 输出：标题（≤75 字符）、五点描述（每条 ≤150）、Item Highlights（≤125）、后台搜索词
- 参考竞品真实品牌命名风格，但**不编造品牌**：未提供则统一 `[待补品牌名]` 占位

**FR-4 四维质检**
- CDQ（内容质量）、A9（搜索 SEO）、COSMO（人群相关性）、Alexa（口语问答命中）
- 输出综合分（0–10）+ 各维度分 + 问题清单 + 优化建议

**FR-5 多轮回炉闭环**
- 生成 → 质检 → 再生成，最多 **2 轮**（质量与延迟权衡，详见第 9 节），由"多轮择优"逻辑选最优轮次输出

**FR-6 诚实 AI / 回问机制**
- 缺失硬事实（品牌/材质/电池/尺寸/适龄/认证）统一 `[待补XX]` 占位并记入 `needs_clarification`
- 严禁编造参数、品牌、认证

## 5. 非功能需求（NFR）

| 编号 | 维度 | 目标（待实测填充真实值） |
|------|------|--------------------------|
| NFR-1 | 性能 | **竞品服务（实测 2026-08-27，4 类目）**：缓存命中 2–7s（儿童自行车 5.8s / led灯 2.0s / 游戏手柄 6.1s）；首次现爬 39–45s（不锈钢杯 39s / 儿童自行车 44.5s / led灯 42.5s）。全链路 Dify（2 轮 + 竞品缓存命中）≈ 60–90s，瓶颈在模型调用；首次现爬叠加 ≈ 100–150s。模型档位/网络波动下以 Dify 实测为准 |
| NFR-2 | 准确率 | 类目解析命中率目标 ≥ 95%（14 类中英文验证全通过，待扩大样本） |
| NFR-3 | 成本 | 单次完整流程目标 < 0.05 元（DeepSeek 档，待实测） |
| NFR-4 | 可用性 | 免费实例保活（UptimeRobot 5min ping），可用率 > 99% |
| NFR-5 | 合规 | 标题/五点/Highlights 长度硬约束由**代码兜底**，不依赖模型自觉 |

## 6. AI 专项设计（产品决策重点）

### 6.1 幻觉红线 / 诚实 AI
- **原则**：品牌、材质、电池容量、尺寸、认证等硬事实缺失时**回问用户**，不编。
- **决策依据**：上架编造参数 = 违规风险，对卖家有害；**诚实回问优于流畅幻觉**。

### 6.2 Human-in-the-loop 边界
- 类目低置信 → 人工确认
- 硬事实缺失 → 回问用户补充
- 质检不过 → 标注需重写的维度，引导补料

### 6.3 评估体系（如何衡量生成质量）
- 四维评分定义（CDQ / A9 / COSMO / Alexa）及各自检查项与权重
- 问题分级：P0（硬门槛违规，如长度超限、禁词）、P1（竞争力不足）、P2（优化建议）

### 6.4 定位说明：Workflow vs Agent
- 当前为 **LLM 工作流**（固定节点 + 回炉循环），非自主 Agent。
- **取舍理由**：工作流可解释、可控、易调试，适合 MVP 快速验证；真 Agent（自主规划 + 工具调用）列入路线图，作为能力演进方向。

### 6.5 案例：诚实 AI 与"丰满表达"的平衡
- **问题**：纯"诚实占位"的产出骨架偏干，观感上不如通用范文工具（如竞品直接套模板把参数填满）。曾因此被质疑"不如别人直接给全文"。
- **决策**：增强生成提示词，在**不突破幻觉红线**前提下提升文案丰满度——Bullet 采用【模块式小标题】写法、加入用户视角（家长/孩子）与场景描写（公园、生日礼物）、给出示范句式；硬事实缺失处仍严格 `[待补XX]` 占位、品牌禁写无意义词。
- **取舍逻辑**：用"表达技巧"弥补信息缺口，而非"编造参数"弥补——既专业可信，又规避上架违规。这是 AI 产品在**可用性（表达丰满）**与**安全性（不编造）**之间的典型平衡决策，也是本产品的核心设计立场。

## 7. 技术架构（简述，给研发对齐）

- **入口/编排**：Dify Chatflow（可视化工作流）
- **竞品服务**：Render 部署 Python 服务 `/compete`（类目解析 + BSR 抓取 + 本地缓存）
- **数据层**：本地 JSON 缓存（`data_cache/`），无外部数据库依赖
- **模型层**：DeepSeek（生成 + 质检），支持切换供应商
- **保活**：UptimeRobot 定时 HEAD ping

## 8. 数据指标与成功标准（实测 + 估算）

**延迟（实测，竞品服务 `/compete`，2026-08-27）**
- 缓存命中：2–7s（样本：儿童自行车 5.8s、led string lights 2.0s、游戏手柄 6.1s）
- 首次现爬：39–45s（样本：不锈钢保温水杯 39s、儿童自行车 44.5s、led string lights 42.5s）
- **优化项（2026-08-27 已提交）**：修复中文类目 `resolve_from_directory` 子串命中 + 缩短抓取重试退避。修复后**中文类目二次请求同英文一样直接命中目录（high）→ 跳过现搜反爬 15–25s**，且 node 固定不再漂移 → 数据缓存稳定可命中。演示前 `python warm_cache.py` 预热一次即永久固化。
- 全链路 Dify（2 轮回炉 + 竞品缓存命中）：≈ 60–90s；若竞品未预热（首次现爬）≈ 100–150s。瓶颈在模型调用次数，非架构。

**待 Dify 实测补全**
- 类目解析命中率（14 类中英文验证全通过，待扩大样本）
- 单次调用 Token 成本 / 金额
- 质检通过率（首轮 / 末轮）、P0 触发率
- 用户补料后复跑通过率

> 注：量化指标用真实运行数据支撑结论。延迟已实测填数；成本与通过率需在 Dify 实跑后补全。

## 9. 风险与权衡

| 风险 | 应对 |
|------|------|
| 免费实例冷启动慢（~50s） | UptimeRobot 保活 + `warm_cache.py` 热门类目预热缓存（演示前批量落缓存，命中 2–7s） |
| 回炉轮数与延迟权衡 | 由 3 轮降为 2 轮：减少约 2 次模型调用（~40–60s），质量损失可控（多轮择优仍选最优）；如需更高通过率可回到 3 轮 |
| 个别类目缓存未命中（实测 不锈钢保温水杯 二次仍现爬 42s） | 疑似该类目抓取落缓存异常，已用预热脚本前置触发；根因待排查（反爬/落盘失败） |
| 模型余额/限流（如 402） | 多模型供应商兜底（如 SiliconFlow） |
| 亚马逊反爬（503） | 完整浏览器头 + 本地缓存 + 限速 |
| YAML 工作流脆弱 | 版本管理 + 导入前结构校验 |

## 10. 后续路线图

1. **热门类目预热缓存（已完成）**：`warm_cache.py` 批量落本地缓存，演示前运行即命中 2–7s，降延迟降成本
2. **真 Agent 版**：自主规划"竞品分析周报"（抓榜→抓详情→对比→建议）
3. 评估体系自动化（批量评测集 + 回归）
4. 多语言能力 / A-B 测试联动

---

*本文档为作品集用途，量化指标章节需以真实运行数据替换"待实测"占位。*
