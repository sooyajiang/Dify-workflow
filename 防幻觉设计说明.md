# 防幻觉设计说明（亚马逊 Listing 生成 Chatflow）

> 定位：作品集可复用设计文档。说明 Chatflow 如何在「用户未提供产品参数」时**既保证有输出、又不编造事实**，并把缺失项转化为「补全真实数据」的可执行建议。
> 关联文件：`amazon_listing_kb_rules.md`（A2.1）、`Dify_prompts_v2.md`（LLM A 第 2.5 步 / LLM B 维度 9-10）、`Dify_代码节点_最终版.py`（render 的 ⚠️/💡 两框）。

---

## 一、问题背景

Listing 生成依赖三类输入：用户输入、竞品真实数据（`comp_context`，来自 BSR 实时抓取）、类目知识库。

**风险点**：当用户只说 "kids bicycle"、不提供材质/尺寸/规格时，LLM 会默认从类目知识库里**自己编造**具体参数（如 "High Carbon Steel Frame" / "16 inch"）填进标题与五点。这违反了亚马逊官方红线「禁止虚构材质、认证、功效、适配型号」，也在面试中容易被追问露怯。

原设计只在知识库 A2 写了「禁止虚构」的合规红线，**但未做成生成前门禁 + 质检后核查的闭环**，靠 `comp_context` 真实数据兜底才没暴露——属于隐性漏洞。

---

## 二、设计原则

### 1. 硬事实 / 软卖点二分
- **硬事实**（可编造、必须溯源）：材质 / 尺寸 / 规格 / 认证 / 数量 / 具体参数。
- **软卖点**（安全、可常写）：适用人群 / 使用场景 / 定性利益（如 "safe & stable for beginners"）。

### 2. 事实溯源门禁（三级来源优先级）
硬事实写入前必须能追溯到以下之一，否则不得写成确定性陈述：
1. **用户输入**（最高优先）
2. **竞品真实数据**（`comp_context` 中抓到的真实商品参数，本 demo 已接入 BSR 抓取）
3. **类目共识**（如"儿童自行车普遍配辅助轮"）——仅作"通用类目认知"措辞，不得伪称本商品专属参数

无依据 → 该硬事实从字段**省略**，改用软卖点替代，**绝不为凑满字段虚构参数**。

### 3. 有需求才建议补全数据（关键约束）
缺失项**不总是**要催用户补数据。仅当满足以下之一才在 `data_suggestions` 给出「补全真实数据」的可执行建议：
- 缺失的硬事实属于**本类目关键决策属性**（见 KB C 层 6 维度属性标签 / 高频卖点词，如儿童车的车架材质 / 轮径 / 辅助轮 / 刹车）；
- 或用户**点名但未给值**。

非关键属性缺失 → 安静省略，**不触发建议**（避免噪音）。

---

## 三、实现落点

| 层 | 文件 | 改动 |
|---|---|---|
| 知识库 | `amazon_listing_kb_rules.md` A2.1 | 事实依据门禁规则（来源分级 + 有需求才建议） |
| 生成 LLM A | `Dify_prompts_v2.md` 第 2.5 步 + 输出字段 | 硬/软二分、门禁指令、`needs_clarification` / `data_suggestions` 两字段 |
| 质检 LLM B | `Dify_prompts_v2.md` 维度 9-10 | 维度 9：无依据硬事实 → 违规 [官方·A2]；维度 10：关键缺失未提示 → 扣分 [共识] |
| 渲染 | `Dify_代码节点_最终版.py` render() | 新增 ⚠️ 待补全（无依据未编造）、💡 数据补全建议 两框（无则跳过） |

**契约不变**：新增字段为纯增量，旧输出无此字段时代码节点自动跳过，下游 demo 行为零侵入。

---

## 四、用户可见输出示例

当输入 "kids bicycle" 且未提供材质/轮径时，生成结果末尾会显示：

```
⚠️ 待补全（无依据，未编造）
> frame_material；wheel_size

💡 数据补全建议
> 关键属性 frame_material 缺失，建议提供真实参数或接入 Best Seller 抓取以确定，避免编造
> 关键属性 wheel_size 缺失，建议提供真实参数或接入 Best Seller 抓取以确定，避免编造
```

而标题/Highlights/五点仍正常产出（用软卖点填充），**不会出现空白，也不会出现编造参数**。

---

## 五、为什么对面试有价值（AI PM 视角）

1. **防幻觉是可解释的工程决策**，不是玄学：把"不编"落成"来源分级 + 缺失即省略 + 显式标记"的可验证规则。
2. **闭环可讲**：生成门禁（LLM A）→ 质检核查（LLM B 维度 9-10）→ 渲染提示（代码节点），三层卡住幻觉。
3. **接回真实数据链路**：`data_suggestions` 指向本作品集已有的「BSR 抓取 → 落飞书 → 派生知识库 → 生成」能力，体现"工具化 / Agent 思维"——不是让模型猜，而是引导补全真实数据。
4. **贴合业务痛点**：跨境电商 Listing 虚构材质/认证会触发亚马逊合规下架，这是真实的运营风险，设计直接对应业务价值。

---

## 六、验证方法（用户侧）

1. 把 KB 重新上传/同步进 Dify 知识库并发布；提示词复制进节点 3（LLM A）/ 4（LLM B）并发布。
2. 用空参数输入（如只输入 "kids bicycle"）跑一次。
3. 预期：标题/五点正常产出、`data_suggestions` 仅对关键属性触发、非关键属性安静省略、代码节点渲染出 ⚠️/💡 两框。
4. 截图存档作「防幻觉前 vs 后」对比证据。
