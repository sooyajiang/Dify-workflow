# 作品集方案总览（竞品监控 + Listing 生成 · 飞书原生闭环）

> 定位：面向上海 B 端 AI PM / Agent / 流程工具化方向的作品集。核心叙事 = **「用户用自然语言说一款商品（任意类目）→ 系统自动匹配类目 → 自动爬该类目 BSR 竞品 → 结合亚马逊新规生成合规 listing + 质检」**，并叠加「每天自动监控竞品 → AI 标变化 / 出洞察」。全部跑在飞书生态内，现场 demo 直观、可署名、可讲架构。
> 本文为总纲，记录需求、架构、真实状态、设计决策、执行方案。任何改动先与用户对齐，不漂移。

---

## 一、需求（对齐基线，不漂移）

### 1.1 终极目标
转岗 **AI 产品经理**，瞄准上海 **B 端 / 公司内部 Agent / 流程与工具化** 方向，发挥 **3 年跨境电商运营** 业务优势。作品集三件套作面试证据。

### 1.2 Dify Listing Chatflow 两条铁律（用户原话）
- **自然语言输入**：用户只说一句话描述商品，**不填表**。这是产品内核，不可改。
- **多类目 = 所有 / 任意类目（非固定白名单）**：用户输入后做**动态类目匹配**，系统按匹配到的类目去**自动爬 BSR 数据**，再生成 listing。不是写死 kids bicycle，也不是预先定义固定类目列表。

### 1.3 类目匹配防错（已拍板）
- 真实类目树**逐级下钻** + **搜索面包屑交叉验证**；
- **置信度门控 HITL**（低置信 / 未知类目走人工确认，不瞎编）；
- **LLM 只做归一化**（把用户话映射到真实 Amazon 类目节点，不让 LLM 凭空造节点）。

### 1.4 其他约束
- 助手看不到用户截图，沟通靠文字 / 报错贴出。
- 用户 git / 命令行小白，步骤需可照抄。
- 不接受拍脑袋的局部指令，先全局对齐再执行。

---

## 二、四层架构（采 → 存 → 智 → 展）

| 层 | 技术选型 | 状态 | 说明 |
|---|---|---|---|
| ① 采集 | `run_pipeline.py`（GitHub skill） + 飞书 bot（App `cli_aae6d6fabe3adbe8`）+ 每日 18:00 自动化；**类目由匹配结果驱动，支持任意类目** | ✅ 已上线 | 抓取服务已部署 Render 云（`https://bsr-monitor.onrender.com`，含 `/run` `/query` `/report`）；飞书 Base 每日 18:00 定时 POST `/run` 自动抓取 + 推日报到群，关机照跑、不依赖本地；架构支持任意类目（目录驱动 + 动态解析），当前目录仅 `kids bicycle` 为 active 种子、每日爬它；按匹配类目实时爬为流 B（见 §六） |
| ② 存储 | 飞书多维表格 Base（14 字段，按日 / 按类目追加） | ✅ 已建 | 低代码关系库，本身就是运营 DB |
| ③ 智能 | Dify Chatflow：Listing 生成 + 质检（主）；监控智能（异常检测 / NL 查询 / 周报） | 🟡 POC | 生成受 7 月新规约束；质检 8 维 |
| ④ 展示 | 飞书仪表盘 / 看板 | ❌ 待建 | 趋势、价格带、品牌占比、异常清单 |

> **闭环主链路（作品集主讲点）**：`用户输入(自然语言) → 动态类目匹配 → 自动爬 BSR(按匹配类目) → 落飞书 Base → Dify 生成 listing：静态规则走 Dify 数据集 + 动态竞品走 /query 工具节点(实时取飞书Base该类目数据) → LLM A/B + 质检 → 回复`（检索架构见 §3.5）。三件套借此串成一条管道，覆盖工程 / 产品 / AI 三面。

选型理由（面试讲点）：存储与展示解耦；先用飞书原生闭环最快验证需求、契合运营受众；生产可平滑换 PostgreSQL + BI，接口不变。

---

## 三、三件套在架构里的角色

1. **skill（GitHub）= 采集引擎**：爬虫 + 飞书 bitable 写入，硬核工程交付物（可署名 / 复现 / 讲架构）。
2. **飞书应用 = 部署态产品 / 触发面**：现场 demo「说话 / 触发 → 飞书出数据」最直观，但平台锁定、需飞书账号。
3. **Dify = 智能层（双用途）**：
   - (a) **竞品监控智能**：吃 Base 历史 → 异常检测 + 自然语言查询 + 周报；
   - (b) **Listing 生成 + 质检（本作品集最"AI"的样板）**：吃「类目匹配后自动爬得的 BSR 竞品 + 通用新规」→ 生成 listing → 8 维质检。
4. **飞书 Base = 存储；飞书仪表盘 = 展示**。

> 相对旧「实时抓取 API」方案：不再单独部署 FastAPI，直接以 Base 作为数据层，Dify 通过 Tool / API 读 Base 即可。更省、更闭环。

### 3.5 数据流转与检索架构（路线 A，已定稿）
- **爬什么**：亚马逊 Best Sellers 竞品，落飞书 Base 共 **14 列**（抓取日期 / 类目节点 ID / 排名 / ASIN / 品牌 / 标题 / 价格 USD / 评分 / 评论数 / 类目内 BSR / 五点描述 / 商品描述 / 链接 / 数据质量）。核心 = 某类目 Top N 竞品的**标题 + 五点 + 卖点 + 价格带**。
- **落库**：`run_pipeline.py` 写**飞书多维表格 Base(bitable API)**，同时落本地 JSONL/CSV；**继续写飞书**（它是云上竞品库，Render `/query` 直接读）。
- **关键事实**：飞书 Base **≠** Dify 知识库。Dify「知识检索」节点只认「上传的 Dify 数据集(dataset)」或「外部知识 API」。
- **推荐检索架构（路线 A）**：
  - 静态通用知识（7 月新规、质检红线，全类目通用）→ 留在 **Dify 数据集**，上传一次，不变；
  - 动态竞品（每个类目最新 BSR）→ **Dify「工具节点」调 Render `/query`（按匹配类目过滤）**，返回竞品文本作 `context` 喂 LLM A。即"实时动态检索" = 不往 Dify KB 灌、生成时现调 `/query`。
- 路线对比：A = 工具节点调 `/query`（推荐，最贴实时、改动小）；B = 真·Dify 外部知识 API（向量检索服务，v1 不必）；C（别走）= 每次爬完追加进 Dify dataset（会乱 / 慢 / 多类目污染）。
- **流 B 待补工程**：`/run` 加 `category` 入参 + 动态节点解析；`/query` 加 `?category=` 过滤。接通即"说任意类目 → 匹配 → 现爬 → 实时检索生成"真闭环。

---

## 四、当前真实状态（诚实版）

| 项 | 状态 | 备注 |
|---|---|---|
| 采集管线 + Base 写入 | ✅ | 20/20 价格评分有值，18/20 评论数；UA 轮换 / 延迟 / 抖动 / 幂等已做 |
| 每日 18:00 自动化 | ✅（8/14 恢复） | automation-1785815069866（原 1785406065429 已失效）；曾因 PAUSED 停在 8/11，已恢复 |
| Dify listing Chatflow | 🟡 指南就绪 | 节点搭建指南已交付（`Dify_Listing_Chatflow_搭建指南.md`，检索架构见 §3.5；生成节点已支持**按需部分生成**：自然语言指定只出某块）；Dify 内仍是**旧结构化版**，待改成「自然语言 + 类目匹配 + /query 竞品 + 按需输出」 |
| 竞品监控智能层（飞书推送） | ✅ 已上线 | `feishu_report.py` + Render `/run` 自动推日报到「竞品监控日志」群（8/14 打通）；`monitor_intelligence.py` 为异常检测 / 周报原型 |
| 飞书仪表盘 | ❌ | 需按 §八设计稿在飞书 UI 搭建 |
| 多类目（动态匹配 + 自动爬） | 🟡 规划锁定 | 需求 / 架构已定：**任意类目 → 匹配 → 自动爬 BSR → 生成**；实现待流 B（见 §六） |
| 截图存档 | ⏳ | Chatflow 跑通结果待截图作证据 |

⚠️ 反爬：数据中心 IP 可能被 Amazon 拦；缓解 = 失败兜底返回上次成功数据，架构不受影响。
⚠️ 合规：v1 抓取，PA-API 5.0 为正规升级路径（需账户资质）。

---

## 五、已达成一致的设计决策

- **类目匹配防错**：真实类目树逐级下钻 + 搜索面包屑交叉验证；置信度门控 HITL；LLM 只做归一化。
- **生成质检**：LLM A 受 7 月新规约束；LLM B 8 维；两代码节点清洗（剥 `<think>` + 剥 ` ```json ` + 兜底找 `{…}`）。
- **用户产品信息用法**：自然语言特征 → LLM A 输入 → 扬长避短。
- **多类目 = 开放任意类目**：用户说啥类目就匹配啥、现爬现生成；**不支持固定白名单式预定义**（mountain_bikes / yoga_mat / dog_toy 这类写死的列表已废弃）。`kids bicycle` 仅为已爬好的一份样例数据。
- **飞书原生选型**：务实、用户导向、可演进。
- **按需部分生成（用户 8/17 提出，已落 Dify 指南）**：用户说"优化标题""写五点"等定向需求时，LLM A 先解析 `requested_parts`（title/highlights/bullets/backend 中哪些），只生成被请求部分；未指定则默认全量。代码节点只渲染有内容的字段，避免"每次全吐"。贴合运营真实场景，比固定全量输出更智能——这是让工具从"固定 workflow"更像"懂业务的助手"的关键设计点。

---

## 六、执行方案（两条并行流，无强制先后）

> **顺序原则（已与用户确认）**：以下两条流**没有强制先后顺序**，先做哪条都行；唯一约束是**不返工**——架构必须前向兼容（类目匹配输出格式、KB / Tool 接口稳定），且**最终交付必须贴合需求**：自然语言输入、不填表；多类目 = 所有 / 任意类目；用户说啥类目就匹配啥、自动爬 BSR 再生成 listing。两条流收敛到同一个终态。

### 流 A — Listing 生成闭环（自然语言 + 类目匹配 + 生成 + 质检）
- 改造旧结构化 Chatflow 为：`自然语言输入 → 类目匹配（LLM A 内部归一化到真实类目）→ [静态规则走 Dify 数据集 + 动态竞品走 /query 工具节点取该类目最新 BSR] → LLM A 生成 / LLM B 质检 → 代码清洗 → 回复`（检索架构见 §3.5）。
- 当前以 `kids bicycle` 已爬数据跑通整链（种子数据；`dify_knowledge_kids_bikes.csv` 作 kids bicycle 的静态 Dify 数据集兜底，/query 接通后由其实时取代）；类目匹配架构就位（LLM 只做归一化、不瞎编）。
- 交付：跑通一次 + 两张截图（编排全貌 + 一次完整输出）作作品集证据。

### 流 B — 多类目自动爬闭环（接通 Render，实现"任意类目现爬现生成"）
- 类目匹配防错完整实现（逐级下钻 + 面包屑交叉验证 + 置信度 HITL）。
- 接通自动爬：Dify 新增「工具 / Tool 节点」调 Render 公网端点 `https://bsr-monitor.onrender.com/run?token=sooya1030&category=<匹配类目>`，按匹配类目触发 BSR 爬取 → 落 Base / 知识 → 生成 listing。
- **代码侧待补（流 B 工程）**：① `serve.py` 的 `/run` 增加 `category` 入参；② `run_pipeline` 支持按指定类目爬（当前按 `category_directory.json` 目录全爬，仅 `kids bicycle` 为 active 种子）；③ 任意类目动态解析（用户话 → 真实 Amazon 节点 → 可爬 URL，禁止编造 node_id）。
- 飞书仪表盘搭建（见 §八）。

---

## 七、依赖与前提

- **本次收尾范围（已定）**：先搭好自然语言版 Dify Listing Chatflow（类目匹配内置 LLM A 归一化 + 检索架构见 §3.5：静态规则留 Dify 数据集、动态竞品走 /query 工具节点；kids bicycle 已爬数据作种子兜底）+ 截图存档；「任意类目自动爬闭环」因 Render 已 live，列为紧随的下一环（Dify Tool 节点接 `/run`），接通成本低。
- Dify 访问权限 + 一个有额度的模型（8/13 曾因 qwen-plus 试用额度耗尽报错；可用 DeepSeek / glm 等便宜模型）。
- 飞书仪表盘为用户侧手动搭建（按 §八设计稿）。

---

## 八、展示层设计（飞书仪表盘，待建）

### 8.1 Base schema（现有 + 衍生）
- 主表 `BSR_快照`（已建 14 字段）：抓取日期 / 类目节点 ID / 排名 / ASIN / 品牌 / 价格 USD / 评分 / 评论数 / 类目内 BSR / 五点 / 描述 / 链接 / 数据质量 / 标题。
- 衍生表 `异常告警`（待建）：日期 / ASIN / 品牌 / 类型 / 严重度 / 描述 / 建议动作。由智能层写入，仪表盘直接引用。

### 8.2 仪表盘视图（飞书看板）
1. **排名趋势**：折线（X = 抓取日期，按 ASIN 分系列，按类目筛选）。
2. **价格分布**：柱状（低价 <$90 / 中 $90–140 / 高 >$140）。
3. **品牌占比**：饼图（TOP 品牌集中度）。
4. **异常清单**：引用 `异常告警` 表，按严重度排序，配筛选。
5. **数据质量**：缺失标记计数（诚信展示）。

### 8.3 AI 智能层（价值核心）
- **异常检测**（`monitor_intelligence.py`）：排名波动 ≥3、降价 / 涨价 ≥5%、评分变化 ≥0.2、新上榜、跌出榜单，输出严重度。
- **自然语言查询**：Dify Chatflow 接 Base（Tool 节点）→ "这周哪些竞品降价了？"直接答。
- **LLM 周报**：异常清单 + 基线 → Dify 润色成一段人话，定时推飞书 / 群。
- 这三者是和普通表格的本质区别，也是面试主讲点。

---

## 九、日后优化（规模化与演进）

- **多租户与权限**：详见 `多租户监控方案.md`。核心 = 单实例共享 Bot（`cli_aae6…`）服务所有运营，不自建多 Bot；给出「自助订阅（模式 A）」与「管理员分配 + 行权限（模式 B，推荐近期）」两种模式及演进路径。当前 demo 单用户，此文档作未来规模化备用。
- **存储演进**：类目 / 数据规模上来后，Base 可平滑换 PostgreSQL + BI 大屏，采集 / 智能 / 展示层接口不变。
- **云端部署（已落地 Render）**：抓取服务已部署在 Render 云服务器（`serve.py` / `run_pipeline`），关机照跑、团队可共享，不依赖本地客户端在线。飞书内跑不了浏览器，故抓取计算在 Render 完成。后续重点是把它与 Dify「工具 / Tool 节点」打通，实现「类目匹配 → 云端爬 BSR → 生成」自动闭环，而非另起云架构。
- **Bot / Skill / API 身份解耦**：当前 WorkBuddy 飞书集成槽只能绑一个 Bot，而 Skill 写 Base 用的是另一个 Bot 凭证；生产化需走 webhook 桥接或统一 Bot 身份。详见 `飞书API与Bot集成说明.md`。

---

## 十、Agent 化路径与求职差距分析（2026-08-17 战略对齐）

### 10.1 最初 v3 设计 vs 当前版本（谁更像 agent）
- 用户最初的 `AI跨境电商运营助手_优化版v3.pdf`（15 页设计手册，已存 `refs/`）已用 Dify「**Agent 应用类型**」+ 规划了「质检→重生成闭环（最多 3 轮）」+「多轮记忆（存最近 5 轮）」+ memory 模块（历史错误 / success_patterns / avoid_patterns）。说明**最初直觉就含 agent 雏形**，不是从零开始。
- 但 v3 是**表单变量输入**（title/category/keywords…），与用户核心需求「自然语言不填表」冲突。
- 当前版本为满足「不填表」改成**自然语言 Chatflow**，做了 UX 内核；但**暂时没把 v3 里两个 agent 味关键设计（重生成闭环、多轮记忆）捡回来**。
- 结论：v3 在「agent 味」更强（Agent 类型 + 闭环 + 记忆），当前版在「UX 核心」更强。**目标 = 在当前自然语言 Chatflow 上补回「重生成闭环 + 多轮记忆」→ 同时拥有自然语言 UX + 反思闭环 + 记忆**。

### 10.2 与「真 Agent」的差距（4 维度）
- **确定性工作流（现在）**：节点顺序写死，LLM 只在定点被调，输入输出固定、可预测。
- **真 Agent**：编排大脑（LLM）在运行时**动态决定**下一步 / 调哪个工具 / 是否继续。四大特征：①动态规划 ②工具自主决策 ③跨轮记忆 ④反思-迭代闭环。
- 当前进度对照：①动态规划 ❌ ②工具自主决策 ❌ ③跨轮记忆 ❌（当前单轮按需） ④反思闭环 ❌（已规划未实现）。
- **准确讲法（面试）**：「LLM 工作流 + 双 LLM 质检闭环，架构预留 Agent 化空间」，**不要硬称 agent**——面试官（尤其 B 端 PM）更看重你懂 workflow vs agent 的取舍，这本身就是真实 PM 判断力，比堆一个假 agent 强。

### 10.3 还有机会加吗（明确：能，三档递进）
越往后越「真 agent」，按成本与必要性递进：
- **第 1 档（低成本，强烈建议，兼最高性价比 agent 化）**：在现有 Chatflow 加「质检 < 阈值 → 带意见回 LLM A 重生成 → 再质检（最多 2–3 轮）」+ 多轮记忆（对话历史节点）。→ 获得「反思闭环 + 记忆」，从 workflow 升为「带反馈闭环的 AI 系统」。**这恰好是 v3 当初规划过、又被当前版暂时丢掉的**。
  - **技术现实（诚实）**：Dify Chatflow 是 **DAG（无回边）**，没有原生 while 循环；第 1 档用「条件分支展开重试链」（A1→B1→判断→A2→B2→判断→A3，失败回灌 `suggestion`）实现，最可靠。具体搭建步骤 + 已验证(5/5)的多轮择优代码节点见 `Dify_Listing_Chatflow_搭建指南.md` §九。
- **第 2 档（中成本）**：切到 Dify「Agent 应用类型」或加「编排 LLM 节点」做动态路由——LLM 据用户输入决定先匹配类目 / 先取竞品 / 先生成，工具自主决策。→ 获得「动态规划 + 工具自主决策」。
- **第 3 档（高成本，作品集非必）**：代码级 Agent（LangGraph / AutoGen）带规划循环 + 工具注册。→ 完全自主 agent，但 PM 作品集通常不必要，且偏离「飞书原生、可现场 demo」优势。

### 10.4 求职 AI 应用 PM：差距 + 优化路线图
**已有护城河**：3 年跨境运营 domain（亚马逊规则 / Listing / BSR / 竞品）——纯技术 PM 没有；作品集三件套覆盖工程 + 产品 + AI 三面；流程工具化思维。

**差距（按优先级）**：
1. 技术理解仍在应用层：缺 LLM / RAG / Agent 系统性认知 + **eval（效果评估）体系未建**——质检目前是「评分非自动打回」，说明还没把「质量可度量」当产品能力。
2. 缺真实 AI 项目 0→1 的 PM 闭环证据：作品集是「自己做的 demo」，不是「有用户、有指标、上线复盘」；简历缺需求分析→PRD→指标口径→上线复盘叙事，缺 AB 实验 / 量化结果。
3. 企业级 B 端视角浅：权限、数据安全、私有化、与现有系统集成等真实场景理解还来自自身作品，缺行业标杆参考。
4. 叙事偏技术实现、弱业务价值：每个作品需补「痛点-方案-价值」三段，尤其 ROI / 量化结果。

**优化（按性价比排序）**：
1. 做「质检→重生成闭环」（最高性价比，兼最低成本 agent 化，见 §10.3 第 1 档）。
2. 把「按需部分生成」升为多轮对话 + 记忆（运营连续「先改标题→再调五点」上下文连续），补足 Agent 的跨轮记忆，且贴合真实使用。
3. 建 eval 体系：拿 10–20 个真实商品做评测集 + 自动打分（标题合规率、质检分分布），证明「不是玩具，是可度量质量的产品」——区分玩具与产品的关键，也是 PM 专业度。
4. 业务价值量化：人工写一条合规 listing 约 X 分钟 vs 工具 Y 分钟；录入 50 个竞品人工 Z 小时 vs 自动爬 N 分钟。
5. 叙事升级：每个作品配「痛点-方案-价值」三段 + 一句「我作为 PM 的关键决策」（如为什么选飞书原生、为什么 RAG 用 `/query` 而非灌库）。
