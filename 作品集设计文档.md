# 多类目亚马逊竞品监控 + AI Listing 生成质检系统

> 作品集项目 A · 定位：AI 应用样板（Dify Chatflow，含 LLM）
> 一句话：用户说一句任意自然语言类目（中/英/整句）→ 实时抓亚马逊 BSR 竞品 → 生成合规 Listing + 四维质检，缺硬事实不编造、补了就过。

---

## 一、系统定位与边界（面试先讲这个）

- **多类目 = 任意自然语言实时解析**，没有预定义类目白名单、不要求用户填表。用户只输入一句话，类目由服务端当场解析（无缓存则现爬），全程不维护类目清单。
- **两条铁律**（也是这个项目的核心设计约束）：
  1. 自然语言输入，不填表、不预选类目。
  2. 多类目 = 所有/任意类目；类目由 LLM 归一化 + 真实类目树逐级下钻 + 搜索面包屑交叉验证得到，**绝不编造 node_id**；低置信/未知类目走人工确认（HITL）门控。
- **与飞书项目的边界**：本项目（Claw/Dify）竞品数据存本地缓存，**不写不读飞书**。另一个独立的 `amazon-bestseller-to-feishu` skill 才是飞书每日推群监控，二者只在"都爬 Amazon BSR"能力上同源，存储/推送完全独立、互不引用。

---

## 二、系统架构

### 2.1 数据流（项目 A）

```
用户自然语言类目
   │
   ▼
Dify Chatflow「开始」节点 (sys.query)
   │
   ├─→ 知识检索（可选，站内规范/品牌库）
   │
   └─→ HTTP 请求 /compete
          │  GET https://amazon-competitor-api.onrender.com/compete
          │      ?token=sooya1030&category=<自然语言>
          ▼
       Render 服务 (amazon-competitor-api)
          ├─ 查本地缓存 data_cache/<node>.json ──命中──► 秒回 (_source:"cache")
          └─ 未命中 ──► 现爬亚马逊 BSR ──► 存本地缓存 ──► 回
          │
          ▼
       返回 comp_context = { stats, brands, anomalies, latest, ask, needs_clarification }
   │
   ▼
Dify「裁剪」代码节点：从 body 提取上述字段 → 输出 comp_context
   │
   ▼
LLM A 生成 → 解析 → if-else① → LLM B 质检 → if-else②(passed?) → 多轮择优 → 输出
```

### 2.2 Dify 工作流真实拓扑（闭环已完整）

```
start ┬→ 知识检索 ─────────────┐
      └→ comp_context HTTP → 裁剪 ┴→ LLM A(生成)
                                │
                                ▼ 解析code
                            if-else①
                  ┌────────────┴────────────┐
               true(一次过)              false(有P0/未过)
                  │                          │
                  │                          ▼ LLM② 二次生成
                  │                     LLM③ 质检 → 解析
                  │                          │
                  │                     if-else②(passed?)
                  │                  ┌───────┴───────┐
                  │               true            false
                  │                │               ▼ LLM④ 再生成 → LLM⑤ → 输出
                  │                ▼
                  └──────────► 输出（多轮择优代码节点选最优轮次）
```

- **最多回炉 3 轮**，第 3 轮后强制输出，杜绝死循环。
- "多轮择优代码"节点按综合得分选最优轮次，并对长度做**代码级硬兜底**。

---

## 三、6 个可讲的设计决策（鲁棒性 / 架构亮点）

1. **类目交叉验证防错**：`crawl_category.py` 的 `parse_search_node` 用面包屑 / zgbs / node 链接三源交叉验证 node_id，绝不凭空编类目节点；已知污染节点（如 Gift Card `2238192011`）显式过滤。
2. **反爬兜底（503）**：亚马逊详情页有强反爬，直接用 requests 会被 503。解析请求统一换**完整浏览器头**（UA + 全套 header），打破反爬，根治"中文类目 400"问题。
3. **长度硬约束由代码兜底，不靠模型自觉**：deepseek 回炉后仍偶发不守"单条≤150字符"，导致 P0 没过。在汇总节点内联截断逻辑：`title≤75 / highlights≤125 / bullets 每条≤150`，并清掉质检报告里的"长度类 P0"误报。改动只动一个节点代码、不碰连线，导入零风险。
4. **多轮回炉闭环**：生成→质检→再生成，逐条处理上一轮反馈，最多 3 轮择优，自动逼近合规而非一错就弃。
5. **免费实例保活**：Render 免费实例会休眠（冷启动 30–50s）。根路径加 `do_HEAD`，用 UptimeRobot 每 5 分钟 HEAD ping，可用率 99%+，冷启动不再拖慢体验。
6. **缺硬事实不编造（诚实 Agent）**：生成时品牌位未给真实品牌则标 `[待补品牌名]`；电池容量/材质/尺寸/适龄等缺失统一用 `[待补XX]` 占位并记入 `needs_clarification`，质检阶段明确回问，**禁止模型编造参数**。

---

## 四、部署与运行

- **后端**：`amazon-competitor-api` 部署在 Render 免费实例；根路径 `GET /` 返回健康检查；`/compete` 需 `token=sooya1030` 校验。
- **保活**：UptimeRobot Monitor 指向 `https://amazon-competitor-api.onrender.com/`（根路径，无需 token），HTTP Method = HEAD，每 5 分钟一次。
- **Dify**：导入 `compete_workflow_fixed.yml` → 发布。用户输入任意类目（如"游戏手柄""不锈钢保温水杯"）即触发全链路。
- **本地缓存**：竞品落 `data_cache/<node>.json`，二次同类目请求秒回，避免重复爬、降反爬压力。

---

## 五、质检维度（LLM B / B2 / B3）

| 维度 | 含义 |
|------|------|
| CDQ | 内容质量 / 结构化属性完整度（材质、电池、尺寸、适龄等硬事实） |
| A9  | 搜索可见性（标题核心词前置、关键词覆盖） |
| COSMO | 人群/场景匹配（目标年龄、使用场景） |
| Alexa | 语音问答可发现性（自然语言问法命中） |

质检输出：状态（✅/❌）、综合得分、硬门槛违规（P0）、各维度评分、问题列表、优化建议。

---

## 六、演示脚本（面试现场）

1. 打开 Dify 已发布应用，输入 `游戏手柄` → 观察：自动解析类目 → 抓取 BSR 竞品 → 出标题/五点/Highlights/搜索词 → 出质检报告。
2. 强调：标题 `Controller` 前置合规、Bullet 不超 150 字符（代码兜底）、质检 ❌ 时回问"请补电池容量/材质"（诚实不编造）。
3. 换一个完全不同类目（如 `不锈钢保温水杯` / `笔记本电脑`）证明"多类目任意输入"不是写死的。
4. 如被问鲁棒性：讲反爬 503 兜底、类目三源交叉验证、免费实例保活三件事。

---

## 七、已知边界与下一步

- **边界**：竞品硬事实（电池 mAh、材质、三维、适龄）模型无法凭空获取，必须由用户/商家补充——这是设计预期，不是 bug；补充后质检即过。
- **下一步可讲的方向**：把 `needs_clarification` 接到表单回问做成多轮交互；或接商品数据库反查硬事实减少占位。
